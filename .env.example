# 网站配置 API Key（可选）
# 如果设置，则 API 请求需要包含 X-API-Key 请求头
# 如果不设置，则 API 公开访问（推荐，因为联系方式本来就是公开的）
SITE_CONFIG_API_KEY=

# Vercel 部署时，在项目设置中添加环境变量：
# 1. 进入 Vercel Dashboard
# 2. 选择项目 → Settings → Environment Variables
# 3. 添加 SITE_CONFIG_API_KEY
# 4. 如果留空，则 API 公开访问
